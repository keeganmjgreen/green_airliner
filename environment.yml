name: electric_airline
dependencies:
  - python
  - pip
  - pip:
      - geopandas
      - ipykernel
      - kaleido
      - matplotlib
      - nbformat
      - pandas
      - psycopg2-binary  # Instead of `psycopg2` with, e.g., Linux `build-essential` package.
      - plotly
      - shapely
      - sqlalchemy
      - numpy-stl
      - pywavefront
      - vpython
      - sphinx
